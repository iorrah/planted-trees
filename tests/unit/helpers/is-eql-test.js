
import { isEql } from 'planted-trees/helpers/is-eql';
import { module, test } from 'qunit';

module('Unit | Helper | is eql');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isEql([42]);
  assert.ok(result);
});

